{
  "models": [
    {
      "provider": "azure-openai-responses",
      "modelId": "gpt-5.4",
      "name": "GPT-5.4",
      "badge": "AZ"
    }
  ],
  "_comment": "provider='azure-openai-responses' uses AZURE_OPENAI_BASE_URL + AZURE_OPENAI_API_KEY + AZURE_OPENAI_DEPLOYMENT_NAME_MAP (e.g. 'gpt-5.4=gpt-54-tower'). provider='openrouter' uses OPENROUTER_API_KEY."
}
