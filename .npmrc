# Prevent NODE_ENV=production from skipping devDependencies.
# PM2 ecosystem sets NODE_ENV=production which propagates to Claude SDK sessions.
# Without this, `npm install` silently skips vite, vitest, tsx, etc.
install-strategy=hoisted
include=dev
